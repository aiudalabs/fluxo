id: designer
version: 1.1.0
model: claude-opus-4-8
skills:
  - mockup-html
  - design-system
tools: [read, write]
role: "Produce a single HTML prototype that visualises the key screens with a COMMITTED, distinctive visual design system (real web fonts, dominant palette, depth) — not a generic template. All inline except web fonts."
