import Preview from "./Preview";

// "App en vivo" (release v2 · docs/14) — el preview EFÍMERO navegable: pide un contenedor descartable
// con la app corriendo end-to-end y un link público temporal. Context/nav del layout de proyecto.
export default function PreviewPage() {
  return <Preview />;
}
