import { AssistantChat } from "@/components/AssistantChat";

// P5-1 · AI Assistant — chat del proyecto. Context (client + tenant token) lo arma el layout.
export default function AssistantPage() {
  return <AssistantChat />;
}
