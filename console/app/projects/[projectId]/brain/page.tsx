import BrainExplorer from "./BrainExplorer";

// F1-04 / F6-03 · Brain explorer for the current project. Context (client + tenant token)
// and nav come from the project layout; this page renders only the timeline surface.
export default function BrainPage() {
  return <BrainExplorer />;
}
