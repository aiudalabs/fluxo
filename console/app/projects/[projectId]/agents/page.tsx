import Agents from "./Agents";

// F6b · Agentes (monitor del conductor). Contexto (cliente + tenant token) y nav vienen del
// layout del proyecto; esta página renderiza solo la superficie Agentes.
export default function AgentsPage() {
  return <Agents />;
}
