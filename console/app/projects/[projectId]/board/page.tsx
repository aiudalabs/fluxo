import Board from "./Board";

// F6-01 · Board for the current project. Context (client + tenant token) and nav come
// from the project layout; this page renders only the Board surface.
export default function BoardPage() {
  return <Board />;
}
