import Flow from "./Flow";

// F6P-04 · Flow para el proyecto actual (vista Ciclo + Grafo). El contexto (cliente +
// tenant token) y el nav vienen del layout del proyecto; esta página solo rinde Flow.
export default function FlowPage() {
  return <Flow />;
}
