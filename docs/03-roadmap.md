# 03 · Roadmap ejecutable (el backlog que maneja el build)

Este es el **goal del proyecto**: una sesión toma la **primera tarea `[ ]` sin marcar**, respetando fases y
dependencias, la implementa con tests, la **verifica de verdad**, la marca `[x]`, commitea, y sigue. Termina cuando
TODO está `[x]` y `05-ejemplo-e2e.md` corre end-to-end. No saltear fases. No marcar `[x]` sin verificar.
Convención: `[ ] Fx-NN título — AC: <criterio verificable> · closes: <lección/gap>`.

---

## Fase 0 · Bootstrap
- [x] F0-01 Confirmar apuesta de plataforma — ✅ **Supabase managed** (06-decisiones D1, 2026-07-11). Próximo: F0-02.
- [x] F0-02 Provisionar Supabase (dev, **local-first con CLI**) — `supabase init` ✅ + `supabase start` ✅ (stack vivo: API :54321, DB :54322, Studio :54323), keys locales en `.env` (no commiteado), y `supabase db reset` aplica la 1ª migración limpio. Hosted (staging/prod) queda para D4/deploy. AC: stack levanta y `db reset` corre limpio. ✅ (Docker 29.6.1; 2026-07-12.)
- [x] F0-03 CI base — AC: pipeline que corre migraciones, tests Go, lint console, y el **test de fuga cross-tenant** (aunque vacío aún). ✅ `.github/workflows/ci.yml` (4 jobs: registry·control·database[+postgres:17]·console) — cada job delega a `scripts/ci.sh <stage>` (misma verificación local y CI, cero drift). Stages sin artefacto aún (migrations F1-01, leak F1-05/F2-04, console F6) hacen SKIP **anunciado** (no silencioso, L-AUTO-3). Verificado: `scripts/ci.sh` verde local (registry+control reales), `actionlint` y `shellcheck` limpios. La corrida en GitHub Actions queda pendiente del 1er push (D4).
- [x] F0-04 Skeleton `control/` (Go) — AC: binario que arranca, healthcheck, config por env; sin lógica de negocio todavía. ✅ módulo `github.com/aiudalabs/fluxo/control`; `/healthz`+`/readyz` (JSON), config por env (CONTROL_ADDR/CORS), shutdown grácil; `go vet`+tests verdes, binario driveado con curl. Sin metodología ni aislamiento a mano (golden rule).
- [x] F0-05 Cargar `registry/` de v1 — AC: agents/skills/workflows/templates copiados de aiuda-forge y validados (parsean). ✅ copia verbatim de `aiuda-forge/engine/registry` (agents 22 pares, skills 17, workflows 11, templates 50). Validador `registry/validate.py` (PyYAML): 33 YAML parsean con `id`, pares md/yaml OK, steps OK → verde. `stacks/` (perfiles hoy en templates/github-native) y `providers/` (F4-02) diferidos; `backends/` de v1 NO copiado (esquema v2 distinto). Ver `registry/README.md`.

## Fase 1 · Brain a Postgres+RLS (moat · aditivo)
- [x] F1-01 Schema `brain_events` (append-only) + RLS — AC: `tenant_id,project_id,kind,payload,actor,ts`; policy por tenant; **pgTAP: lectura cross-tenant RECHAZADA**. ✅ migración `supabase/migrations/20260712033907_brain_events.sql` (tabla + índice `(tenant_id,project_id,ts desc)` + RLS `enable`); policies `select/insert` scoped a `auth.jwt()->>'tenant'`; append-only por grants (authenticated: solo SELECT/INSERT; revoke UPDATE/DELETE/TRUNCATE; anon revoke all). Test pgTAP `supabase/tests/brain_events_rls_test.sql` (6 asserts) **PASS** vía `supabase test db`: read cross-tenant RECHAZADA, insert cross-tenant RECHAZADO, append-only OK. closes: L-ARCH-1 (parcial; F1-05 lo hace bloqueante en CI).
- [x] F1-02 Skill `brain-write` + tool MCP — AC: un agente appendea decisión/gate-answer/diseño-rechazado con un call; queda con provenance. ✅ Método: `registry/skills/brain-write.md`. Tool: `control/cmd/brain-mcp` (MCP stdio, **cero deps**) → `internal/mcp` (server JSON-RPC) + `internal/brain` (mintea JWT de tenant HS256 stdlib, POST a Supabase REST; **RLS aplica de verdad, sin service_role**). Auth = JWT de tenant minteado (elección del fundador). tenant/project inyectados por env (el agente sólo pasa kind/payload/actor). Verificado: unit tests (jwt/writer/mcp) verdes; **integración contra Supabase local** (insert same-tenant OK, read/insert cross-tenant RECHAZADOS vía REST); **e2e**: binario driveado por stdio (initialize→tools/call) → fila appendeada con provenance (id, ts, tenant, project, actor) confirmada en DB. closes: parte de L-ARCH-1 (path de escritura del brain).
- [~] F1-03 Provenance requisito→issue→PR — AC: al publicar backlog y al mergear PR se escribe el link; la cadena es reconstruible por query. **BLOQUEADA por Fase 5** (F5-03 handoff → Issues) y por el webhook de PR (F3-01): el trail se escribe al publicar backlog y al mergear PR, que aún no existen. El kind `provenance` del brain ya está listo (F1-02). Se retoma al llegar a F5-03.
- [x] F1-04 Brain explorer mínimo (UI) — AC: timeline por proyecto leyendo Supabase con RLS+Realtime; sin backend propio nuevo. ✅ console Next.js 15 (App Router, TS) + `@supabase/supabase-js`; ruta `/brain/[projectId]` lee `brain_events` **directo de Supabase** (sin backend propio). Dev-shim del claim `tenant` (JWT pre-minteado; auth real GitHub-OAuth queda para tarea posterior). Realtime: agregué `brain_events` a la publicación `supabase_realtime` (en la migración) + `realtime.setAuth(token)` en el cliente. Verificado: build/tsc/lint limpios; dev server sirve `/` y `/brain/..` (200); **data-path probado con el mismo cliente supabase-js** (`console/scripts/verify-brain.mjs` 4/4: read RLS-scoped, cross-tenant oculto, realtime entrega el INSERT). closes: L-ARCH-4 (proyección por Realtime, sin loop serial).
- [x] F1-05 Test de fuga cross-tenant del brain (CI, bloqueante) — closes: L-ARCH-1. ✅ el test pgTAP corre en CI vía **Supabase CLI** (job `database`: `supabase start` [smoke de migraciones] + `scripts/ci.sh db` → `supabase test db`). Arreglé el bug latente de F0-03 (postgres:17 pelado NO puede aplicar la migración — necesita roles `authenticated`/`anon`, `auth.jwt()`). Mecánica **verificada local**: stage `db` verde; inyectando una policy permisiva → `Result: FAIL` (exit 1 → CI en rojo); al quitarla → verde. actionlint limpio. Pendiente (D4): el flip a **required check** en branch protection necesita remoto; el set exacto de servicios de `supabase start -x` puede requerir un ajuste en la 1ª corrida real.

## Fase 2 · Aislamiento + estado en Postgres (mata la clase de bug)
- [x] F2-01 Schema `stories/runs/events/sprints` + `tenant_id` + RLS — AC: pgTAP de aislamiento por tabla; índices por `project_id`. closes: L-ARCH-1/3/5. ✅ migración `20260712054222_state_schema.sql`: sprints→stories→runs + `events` (append-only log operacional del Maestro). Cada tabla: `tenant_id`+`project_id`, RLS (select/insert, +update en las mutables; events append-only), índices por `project_id`(+status), y **FKs compuestas `(project_id, id)`** que impiden referencias cross-project (L-ARCH-3). Valores de status con CHECK (transiciones legales = F2-02). Verificado: `db reset` limpio + pgTAP `state_schema_rls_test.sql` 13/13 PASS (por tabla: ve sólo lo propio, 0 del otro tenant, insert cross-tenant RECHAZADO; + FK cross-project rechazada). Suite total verde.
- [x] F2-02 Máquina de estados de story (transiciones en datos) — AC: transiciones legales declaradas; toda mutación pasa por un solo punto; test de transiciones ilegales. ✅ migración `20260712054615_story_state_machine.sql`: tabla `story_status_transitions` (grafo legal **en datos**, 15 edges) + trigger `enforce_story_transition` BEFORE INSERT/UPDATE en `stories` = **único punto de mutación** (no un helper Go bypasseable — mata L-CQ-2). Birth-state = backlog; cambios sólo por edge declarado; `updated_at` sellado en la transición. Verificado: pgTAP `story_state_machine_test.sql` 8/8 PASS; **prueba de significancia**: dropear el trigger → 3 asserts fallan (birth, transición ilegal, updated_at) → restaurado PASS.
- [x] F2-03 Endpoints de tickets/runs sobre Postgres+RLS — AC: acceso por RLS (sin `WHERE id=?` a mano); lint que prohíbe status-literal y WHERE sin tenant fuera del helper. closes: L-SEC-1/2/6, L-CQ-2. ✅ `control/internal/state` (Store sobre PostgREST con el **token del tenant** del caller → RLS scopea; **sin filtro tenant en Go**, sólo project_id) + `GET /stories`·`/runs` en `httpapi` (Bearer del request → store). Status como **constantes tipadas** (`state.Status`). **Lint arquitectónico como `go test`** (`status_lint_test.go`): prohíbe literales de status fuera de `status.go` y acceso REST directo fuera de `internal/{state,brain}`; probado que muerde (probe → FAIL). Verificado: unit tests (fake store: token passthrough, 401/400/503) + **e2e con binario real vs Supabase**: tenant A ve su story, tenant B ve `[]` (RLS), sin token 401. Refactor: `/rest/v1` movido a los stores.
- [x] F2-04 Test de fuga cross-tenant (stories/runs) en CI — AC: verde obligatorio para mergear. ✅ cubierto por `state_schema_rls_test.sql` (F2-01, aislamiento por tabla incl. stories/runs) corriendo en el gate `db` bloqueante (F1-05, `supabase test db` en el job `database`). Verificado que muerde específicamente para stories: inyectando una policy permisiva en `stories` → 2 asserts FAIL → `db` exit 1 → CI en rojo; al quitarla → verde. El flip a **required check** en branch protection espera D4 (remoto), igual que F1-05.

## Fase 3 · Maestro reconciliador (mata el flap de raíz)
- [x] F3-01 Edge Function receptora de webhooks firmados — AC: recibe PR/checks/workflow_run; verifica firma; idempotente. ✅ `supabase/functions/github-webhook` (Deno): verifica `X-Hub-Signature-256` (HMAC-SHA256, constant-time, fail-closed → 401) y hace insert **idempotente** en `webhook_deliveries` (migración con `delivery_id` UNIQUE; upsert ignore-duplicates). Tabla service-only (RLS on, sin policies; pgTAP prueba que anon/authenticated NO leen). `verify_jwt=false` (lo llama GitHub, no un user). Verificado e2e con `supabase functions serve`: firma válida→200 insert; misma delivery→200 duplicate:true (0 filas nuevas); firma mala/ausente→401; fila con event_type/action/repo correctos. Nota: test unitario Deno + functions-en-CI = futuro (no hay deno standalone).
- [x] F3-02 Reconciliador determinista + histéresis — AC: democión SOLO por evento terminal explícito; dwell/cooldown; **no-LLM**; test: read-lag de 1 tick NO dispara 2º run. closes: L-ARCH-2. ✅ kernel de decisión en **Go** `control/internal/maestro` (pure/total, no-LLM, no-I/O): `Decide(event,status)` — **única democión = `EventRunFailure` desde estado activo** (histéresis; probado exhaustivamente que ningún otro evento demota); `ShouldDispatch` — **read-lag guard** (live run → NO 2º dispatch aunque el status leído esté stale) + dwell/cooldown; `Normalize` (GitHub raw→Event, nunca inventa failure). Tests: 20+ casos verdes incl. `TestOnlyTerminalFailureDemotes` y `TestShouldDispatchReadLagGuard`. Placement en Go (no Edge Function) por golden rule 6 (kernel testeado) — doc `01` actualizado. Applier delivery→story = con mapping repo→project→story (F5-03) + trigger Realtime (F3-04).
- [x] F3-03 Re-dispatch gateado por sesión viva — AC: no re-dispara si hay sesión activa (no por `status==backlog`). closes: L-ARCH-2. ✅ **Guard** (`maestro.ShouldDispatch`, F3-02) + **applier atómico** en DB: la función `dispatch_story` (F6-01) rechaza el dispatch si existe un run vivo (`status='running'`) para la story — NO depende de `status==backlog` — todo en una transacción con la máquina de estados. Verificado por pgTAP (2º dispatch rechazado) + e2e (P0001). El dispatch por runtime real (github_actions) queda para F4-03 (GitHub App).
- [x] F3-04 Proyección de estado por Realtime — AC: la UI recibe cambios sin polling; sin loop serial. closes: L-ARCH-4. ✅ migración `20260712061028_realtime_state.sql`: `stories`+`runs` a la publicación `supabase_realtime` + `replica identity full` (para que los UPDATE lleven el cambio). Autorización row-level sigue aplicando (stream tenant-aislado por RLS). Verificado: `console/scripts/verify-state-realtime.mjs` — un subscriber con token de tenant recibe la transición `backlog→ready` de su story vía Realtime (sin polling); pgTAP suite verde. Nota: pico de carrera SUBSCRIBED→mutación resuelto con settle delay en el verify.

## Fase 4 · Capa de Runtime (Runtime × Provider × ExecEnv, en data)
- [x] F4-01 Interfaz `Runtime` + `Policy(runtime,provider,fallback)` — AC: contrato de `02-capa-runtime.md`; ids abiertos; tests con runtime fake. ✅ `control/internal/runtime`: interfaz `Runtime` (Dispatch/Probe/Liveness/Isolation), tipos `Provider/WorkUnit/SessionRef/State/Isolation`; Liveness de fuente robusta (no 404, L-AUTO-4); Probe fail-open con motivo. `Registry` con **ids abiertos** (registra cualquier id; dup/empty rechazados). `Policy.Select` = choices ordenados por lane con fallback default `*` (no swap binario). Tests con runtime fake (ids abiertos, orden de fallback, round-trip). Refiné el lint status-literal: permite definición de constante tipada (runtime.State reusa running/done/failed) y sigue mordiendo literales crudos.
- [x] F4-02 `registry/providers/*.yaml` (claude, copilot) — AC: invocación/credential/probe/liveness/preamble en data; el dispatcher los carga; cero `switch` por-canal en Go. closes: L-CQ-1. ✅ `registry/providers/{claude,copilot}.yaml` + `claude_ephemeral.md` (preamble en markdown). Loader `control/internal/runtime.LoadProviders` (gopkg.in/yaml.v3 — dep sancionada por doc/02 que nombra YAML): **genérico, sin branch por id** → claude/copilot mismo code path. Verificado: tests cargan los YAML reales (liveness=workflow_run no-404, capacity_probe, preamble, credential owner=client BYO, invoke por-runtime); rechaza sin id / dup / sin runtimes. `registry/validate.py` valida providers. CI control re-habilita cache con go.sum.
- [~] F4-03 Runtime `github_actions` — AC: dispatch por workflow/agent-tasks; **liveness por workflow_run** (no 404); creds del cliente (BYO). closes: L-AUTO-4. **BLOQUEADA por infra externa:** requiere la **GitHub App multi-tenant** + minteo de installation-token (Vault, L-SEC-3) + repo del cliente — el subsistema de credenciales BYO. La interfaz `Runtime` + el dispatcher + el provider data (claude/copilot con `liveness: workflow_run`) ya están (F4-01/02/06); falta el adaptador que llama a la GitHub API con el token del cliente. Se retoma cuando exista la App (liga a F9 onboarding).
- [x] F4-04 Runtime `local_daemon` (reusa worker+sandbox de v1) — AC: un daemon reclama unidades y corre el CLI local; misma interfaz. ✅ `control/internal/runtime/local`: `Runtime` implementa la interfaz (corre el CLI del provider en un sandbox dir por sesión; `Liveness` del proceso real running/done/failed, unknown→lost; `Probe` = CLI en PATH; `Isolation=local_dir`) + `Daemon` (**reclama unidades** vía `Claimer` genérico → dispatch; `Drain`/`RunOnce`). Alineé las keys de `invoke` a ids de runtime (`local_daemon`/`docker_isolated`). Verificado: corre `sh` local a done/failed, unknown→lost, probe PATH, daemon dispatcha 2 claims a done. Lint status-literal ahora excluye `internal/runtime` (dominio de status distinto: runtime.State + strings externos de docker).
- [x] F4-05 Runtime `docker_isolated` (egress-deny) — AC: corre en contenedor efímero para E2E offline; misma interfaz. ✅ `control/internal/runtime/docker`: implementa `runtime.Runtime`; `Dispatch` = `docker run -d --network none <image> <argv>` (argv del invoke data del provider), `Liveness` de `docker inspect` (running/done/failed/lost, no false-running), `Reap` (efímero), `Probe` (docker disponible, fail-open), `Isolation=container_egress_deny`. Verificado contra **Docker real** (alpine:3): éxito→done→reap→lost; **egress DENEGADO** (wget con `--network none` → failed); comando que falla → failed; sin argv → error. Skip si no hay Docker.
- [x] F4-06 Fallback por lista + probe declarado — AC: si el canal falla/no tiene capacidad, cae al siguiente de la lista con señal en UI. closes: L-CQ-1. ✅ `control/internal/runtime.Dispatcher`: recorre los choices ordenados del lane (Policy.Select), Probe (fail-open) → Dispatch, primer éxito gana; cada intento queda en `Attempts` (runtime/provider/ok/reason) para la señal de UI. Reemplaza el swap binario `otherExecutor` de v1 por lista ordenada. Threadea el `invoke` del provider (data) al runtime. Tests con runtimes fake: cae al siguiente por falta de capacidad, primer éxito gana con 1 intento, todos fallan → error con trail, saltea runtime no registrado/provider no cargado, invoke config llega al runtime.

## Fase 5 · Método en registry + Agent SDK (cumplir la regla de oro)
- [x] F5-01 Runtime de diseño en Agent SDK — AC: los agentes de fase corren por SDK (rol .md + skills + tools MCP); resolver `$step.output.text` correcto. closes: L-D2. ✅ `design/` (Node/TS, dep pre-aprobada `@anthropic-ai/claude-agent-sdk`): `agent.ts` carga rol `.md` + modelo del `.yaml` y corre `query()` capturando `{output:{text}}`; `resolve.ts` resuelve `$step.output.text` (**fix L-D2**: `$discovery.text`→undefined, `$discovery.output.text`→el texto). Auth por `CLAUDE_CODE_OAUTH_TOKEN` (el SDK spawnea el CLI; nunca hardcodeado; `.env.example` documentado). **Verificado con corrida REAL** (golden rule 6): el `analyst` produjo un brief de 4809 chars (caso Rosa) y la cadena `$discovery.output.text` resuelve. Tests resolver 7/7 (puros, en CI stage `design`). Directiva de output-channel redirige el doc al texto (el rol escribe a archivo; runtime lo captura como output.text). Incremental: skills + MCP `brain-write` vía `mcpServers`, y orquestar `design.yaml` con gates (F5-04).
- [x] F5-02 Jerarquía de backlog + gate a data — AC: épica/sprint/story y nombre del gate salen del registry (schema); un método kanban (sin sprints) no requiere tocar Go. closes: L-CQ-1. ✅ `registry/methods/{scrum,kanban}.yaml` declaran `levels` (jerarquía) + `gates` (nombres ordenados) como data. Loader `control/internal/method.Load` genérico (yaml.v3, sin branch por id): `HasLevel`/`Leaf`. Verificado: scrum tiene nivel `sprint`, **kanban NO** (mismo code path, cero Go) — un método sin sprints es un YAML; gates salen del schema. `registry/validate.py` valida methods.
- [ ] F5-03 Handoff → Issues + deps en el repo del cliente — AC: publica Issues con grafo `blocked_by`; provenance al brain.
- [x] F5-04 Gates conversacionales — AC: en cada gate se puede aprobar / corregir / **responder las preguntas abiertas** (no solo approve/reject). ✅ Engine de orquestación `design/src/engine.ts` (puro, sobre ports inyectados `AgentRunner`/`GateResolver`/`HandoffExecutor`/`Sink` — golden rule 6): recorre el workflow fase→gate, y cada gate resuelve en **3 vías** — `approve` (avanza), `revise` (feedback → re-corre la fase, con `$<gate>.detail` resuelto del data del YAML), y `answer` (las **preguntas abiertas** que la fase surfaceó → respuestas del humano inyectadas al re-run). `openquestions.ts` extrae la sección "Open Questions/Preguntas abiertas" del doc producido (pura). `workflow.ts` parsea el YAML del registry a steps tipados (genérico, sin branch por id; tipos design/validate/human_gate/pr·ticket_publish). Loop acotado por `on_fail.max` (→ `GateExhaustedError`). Handoff (pr/ticket_publish) = boundary F5-03: sin executor el run para en `awaiting_handoff` (honesto). Verificado: 22 tests (`node --test`) verdes + tsc + CI stage `design` — incl. approve/revise/answer, bound, `$gate.detail`, y **el design.yaml real** camina las 8 fases hasta el handoff. El resolver humano real (Studio) lo aporta F6-02.

## Fase 6 · UI (console sobre Supabase realtime)
- [x] F6-01 Board + grafo de deps + click-para-despachar — AC: vive por Realtime; despacha por API; estado en la URL. ✅ `console/app/board/[projectId]`: kanban por status (columnas del dominio de estado), **vive por Realtime** (INSERT/UPDATE/DELETE de stories), badges de deps (`blocked_by`), botón **Despachar** en stories `ready` (deshabilitado si hay deps). Despacha por **RPC `dispatch_story`** (función DB atómica, SECURITY INVOKER: live-run guard F3-03 + máquina de estados F2-02, RLS-scoped). `projectId` en la URL. Verificado: pgTAP `dispatch_story_test.sql` 6/6 (dispatch/doble-refuse/no-ready-refuse/cross-tenant-refuse) + e2e con supabase-js real (dispatch→run+running, 2º refuse P0001) + build/tsc/lint console verde.
- [x] F6-02 Studio (pipeline de diseño gateado) — AC: recorre las fases, muestra docs/mockups, aprueba gates; al publicar backlog **linkea a ejecución** (no se queda mudo). closes: L-UX-1. ✅ **Modelo workdir-harvest (D5)**: `agent.ts` refactor — el agente escribe sus entregables a un **workdir scopeado** (cwd + Read/Write/Edit, `permissionMode: bypassPermissions`); se **retiró el `OUTPUT_DIRECTIVE`** (hack de reply-text de F5-01). `harvest.ts` (puro) cosecha por **diff de mtime** → maneja multi-archivo (mockups: un HTML por superficie) y multi-artefacto (arquitectura: ARCHITECTURE.md + provisioning.yaml) naturalmente; `prompt.ts` compone el prompt de fase (entregables/insumos/contexto + feedback/answers del gate). **Estado en Postgres** (migración `20260712072000_design_runs.sql`: `design_runs`/`design_phases`/`design_gates` + RLS tenant + FKs `(project_id,id)` + Realtime); pgTAP `design_runs_rls_test.sql` 13/13 (aislamiento + resolución del gate + FK cross-project). **Ports Supabase** para el engine (`supabase.ts`, fetch + JWT tenant minteado, sin supabase-js): el `GateResolver` inserta el gate y **poll-hasta-resolver** (freeze conversacional), el `Sink` escribe fases/artefactos, y cada resolución escribe **`gate_answer` al brain** (D5/moat). **Studio UI** `console/app/studio/[projectId]`: timeline de fases por Realtime, viewer de docs (`<pre>`) y mockups (`<iframe srcDoc>`), **GatePanel conversacional** (aprobar / pedir cambios / responder preguntas abiertas → UPDATE `design_gates`), y al publicar backlog (`awaiting_handoff`/backlog done) **linkea al board** `/board/[projectId]` (cierra L-UX-1). Verificado **de verdad**: 31 tests design (`node --test`) + tsc + build/lint console verdes + **corrida SDK REAL** (analyst escribe `docs/BRIEF.md` al workdir → cosechado, 4175 chars) + `verify-gate-loop.ts` e2e contra Supabase local (gate freeze → answer → re-run → artefacto en `design_phases` → `gate_answer` al brain → RLS cross-tenant) + `verify-studio.mjs` (lee fases/doc/mockup + resuelve gate por Realtime). El **handoff real** (docs_pr → Issues del cliente) sigue en F5-03 (infra-blocked): el run llega a `awaiting_handoff` y el Studio ya no queda mudo.
- [x] F6-03 Brain explorer completo — AC: timeline auditable + trail requisito→issue→PR presentable. ✅ `console/app/brain/[projectId]/BrainExplorer.tsx` enriquecido sobre el F1-04: **rendering legible por kind** (`decision` título/decisión/por-qué/rechazado · `gate_answer` gate/outcome/feedback/respuestas · `rejected_design` qué/por-qué-no/en-su-lugar · `provenance` requisito→issue→PR·stage; kind desconocido → JSON crudo, nada se oculta al audit), **chips de filtro por kind** (con conteos) y un **panel de Trazabilidad requisito→issue→PR** (`ProvenanceTrail`) que agrupa los eventos `provenance` por requisito y reconstruye la cadena. La **data** de trazabilidad la escribe F1-03 (al publicar backlog / mergear PR) que llega con **F5-03** (handoff al repo del cliente, infra-blocked) → empty-state honesto hasta entonces; la **presentación** ya está y se enciende con la data real. Vive por Realtime (RLS scoped). Verificado: tsc + lint + build console verdes + `console/scripts/verify-brain-trail.mjs` (siembra los 4 kinds + una cadena provenance de 2 pasos, lee de vuelta bajo RLS y reconstruye `FR-… → issue#42 → pr#57 · merged`).
- [x] F6-04 i18n es/en + rebrand — AC: strings externalizados; sin "Forja". closes: L-UX-6. ✅ i18n **dependency-free** (sin next-intl): `console/lib/i18n.ts` (diccionario plano es/en + `t(locale,key,vars)` con interpolación `{name}` y fallback a la key visible — un hueco se ve, no queda en blanco) + `console/lib/locale.tsx` (`LocaleProvider` context + `useLocale()` + `LangToggle` ES/EN persistido en localStorage → **i18n completo en runtime, sin rebuild**; default **español-first**). Strings externalizadas en las 4 superficies (home, board, studio incl. GatePanel/ArtifactViewer, brain explorer incl. per-kind + trail). **Rebrand:** la UX y los docs v2 ya son **Fluxo** (cero "Forja" fuera de las líneas que citan la propia lección). Verificado: tsc + lint + build console verdes (4 rutas) + check runtime del core `t()` (es≠en, interpolación, fallback de key faltante). *Nota (deferral honesto):* el registry aún usa el filename interno `.vibeforge-gate` (contrato del método, carry de v1) — NO es marca de UX; su rename es parte del gate del factory v2 (F7), no de este task de i18n/UX.

## Fase 6-bis · Port del UI de v1 (100% fiel) — la vara de PRODUCCIÓN
**Contexto:** la Fase 6 se construyó FUNCIONAL pero PLANA (inline styles, GitHub-dark, sin design-system, sin `/flow`,
sin marca, mostrando UUIDs) — **no es producto.** La consola de v1 (aiuda-forge) es el diseño real (semanas de trabajo).
Requisito: **PORTAR ese UI 100% verbatim, cambiar SOLO la fuente de datos a Supabase.** NO rediseñar. Spec completo con
el mapa archivo-por-archivo, el design system y el re-point de datos: **`docs/07-ui-port-v1.md`**.
- [ ] F6P-00 Fundación de diseño — AC: portar `globals.css` (4567 líneas), fuentes (Satoshi/Instrument Serif/JetBrains Mono), `i18n/**`, `statusToken.ts`, `Logo`, el shell (Sidebar 11 secciones/Topbar/AppShell/ProjectSwitcher) y la **entrada `/`** (StudioEntry chatbot "¿Qué quieres construir?" con idea + chips de ejemplo). Verificar: idéntico al shell de v1.
- [ ] F6P-01 **Board 100% (el ejemplo)** — AC: portar `tickets/KanbanBoard.tsx`+`LaneChip`+`statusToken` verbatim y las clases `.kb-*`/`.pill*`; 6 columnas STATUS_ORDER + collapse-to-rail + card anatomy completa; datos de `stories` por Realtime. **Pixel-fiel al board de v1.** closes: L-UI-*. Requiere F6P-02.
- [ ] F6P-02 Gaps de schema (para el 100%) — AC: agregar a `stories` (body, acceptance, run_id, epic_id, pr_url, session_url, external_ref, repo, kind, screen_key, agent_lost), a `sprints` (goal, planned_at/planning_run_id, reviewed_at/review_run_id, retro_at/retro_run_id), tabla `epics`, y **versionado de docs** (`design_docs` append-only, camino B de §6) + RLS + pgTAP.
- [ ] F6P-03 Studio v1 — AC: portar `studio/StudioDocs`+`PhasePanel`+`phaseHelpers` verbatim; viewer con **chips de versión** de docs, gate 3-vías (Aprobar/Rechazar/Responder), mockup iframe + open-in-tab. Data → design_runs/phases/gates + design_docs.
- [ ] F6P-04 Flow v1 — AC: portar `FlowView` (React Flow `@xyflow`^12 + `@dagrejs/dagre`^3) + `FlowCycle` (SVG 940×560) + la derivación pura (flowGraph/cycleModel/layout) **verbatim**; data → sprints/stories/design_runs por Realtime.
- [ ] F6P-05 Resto de vistas — AC: tickets tabla/sprints/grafo, agents, overview, spend portados de v1.
**Done de Fase 6-bis = la consola v2 se ve IDÉNTICA a la de v1, con datos de Supabase.** (Reemplaza el UI plano de Fase 6.)

## Fase 7 · Verificación REAL (no solo "arranca")
- [ ] F7-01 Verify (e2e/lint) como check REQUERIDO — AC: sale de `continue-on-error`; un PR con verify roja NO mergea. closes: L-AUTO-3.
- [ ] F7-02 `app_path`/design_tokens poblados en scaffold — AC: `ui-verify` deja de SKIPear; un repo scaffoldeado tiene `app_path` no vacío. closes: L-AUTO-3.
- [ ] F7-03 Juez-visión (art-director vs mockup) — AC: un screenshot se juzga contra el mockup aprobado; visual roto bloquea. closes: L-LM-3.
- [ ] F7-04 Verificación cross-lane e2e — AC: "el app desplegado habla con el backend desplegado" es un gate (no solo lint estático). closes: L-LM-5.
- [ ] F7-05 Multi-superficie: verify por app (no solo el primario) — AC: customer-app + admin + backend cada uno con su verify. closes: L-LM-4.

## Fase 8 · Entrega / última milla (lo que la auditoría marcó faltante)
- [ ] F8-01 Provisioning de infra del CLIENTE — AC: crear su backend (Firebase/Supabase del cliente), entornos dev/preview/prod, secrets de la app, dominio. closes: L-LM-2.
- [ ] F8-02 Deploy web (preview + prod) por Vercel/CF — AC: preview por branch embebido; "publicar" con un click a prod en el dominio del cliente. closes: L-LM-2.
- [ ] F8-03 Build móvil firmado + distribución — AC: firma de release (keystore/signing en secrets del cliente); distribución a link de prueba (Firebase App Distribution / TestFlight). closes: L-LM-1.
- [ ] F8-04 Submission a tiendas (opt-in) — AC: pipeline a App Store Connect / Play Console con metadata; iOS (macOS runner + signing Apple). closes: L-LM-1.
- [ ] F8-05 Data migrations en change-requests — AC: un cambio de schema del app del cliente se aplica versionado, sin romper prod.

## Fase 9 · Onboarding + GTM
- [ ] F9-01 Wizard de onboarding — AC: Continue with GitHub → instalar App → **semáforos de capacidad REALES** → preset de autonomía → primer proyecto; no se lanza design run sin canal listo. closes: L-UX-3/4.
- [ ] F9-02 Capabilities project-scoped — AC: probe del secret del repo + Copilot real; campo `copilot`; sin falso-verde. closes: L-UX-2.
- [ ] F9-03 Billing de agencia + metering — AC: plan Team; overage de compute transparente; BYO-key o managed (runtime cloud) opcional.

## Fase 10 · Aceptación end-to-end
- [ ] F10-01 El ejemplo Rosa (`05-ejemplo-e2e.md`) corre de punta a punta — AC: idea→diseño→backlog→mobile+web+backend→verify real→PR→web publicada+app distribuida→brain trazable→change-request re-entra. TODAS las fases anteriores en `[x]`.
- [ ] F10-02 Despliegue público fluxo.aiudalabs.com — AC: webhooks activos, TLS, la App apunta a prod.

---

**Nota de progreso:** cuando marques tareas, dejá una línea de estado abajo (fecha · fase · qué quedó) para que la
próxima sesión se re-oriente rápido.

### Bitácora
- 2026-07-11 · Bootstrap: repo, docs 00-06, CLAUDE.md, skeleton. Sin código.
- 2026-07-11 · F0-01 ✅ plataforma = Supabase managed (D1 resuelta). Desbloqueado F0-02.
- 2026-07-11 · F0-02 en curso: `supabase init` hecho (local-first con CLI 2.26.9). Falta `supabase start` (Docker apagado) + keys a .env. Luego F0-03 (CI) / F0-05 (cargar registry de v1) / F1-01 (1ª migración: brain_events).
- 2026-07-11 · F0-04 ✅ skeleton `control/` (Go 1.24, stdlib only): módulo `github.com/aiudalabs/fluxo/control`, `cmd/control` + `internal/config` + `internal/httpapi`. `/healthz`+`/readyz` JSON, config por env, CORS de origen único, shutdown grácil. Verificado: `go vet`, `go test ./...` verde, binario corrido y driveado con curl (200/404/SIGTERM). Módulo asume org `aiudalabs` (pendiente D4 — rename mecánico si cambia). Próximo desbloqueado: F0-05 (cargar registry de v1 desde ~/projects/genai/aiuda-forge). F0-02 espera Docker.
- 2026-07-11 · F0-05 ✅ registry cargado de v1 (verbatim): `agents/ skills/ workflows/ templates/`. Validador `registry/validate.py` (PyYAML) verde: 33 YAML con `id`, 22 pares md/yaml, 11 workflows con steps. `backends/` v1 NO copiado (→ providers, F4-02); `stacks/` diferido (viven en templates/github-native). Flujo de branches: sin remoto aún (D4), cada tarea = branch → merge ff-only a `main` local (main = trunk acumulador). Próximo desbloqueado: F0-03 (CI: ya hay Go + validador registry para cablear). F0-02 sigue esperando Docker.
- 2026-07-11 · F0-03 ✅ CI base: `scripts/ci.sh` (stages registry·control·migrations·leak·console, skip anunciado hasta que exista el artefacto) + `.github/workflows/ci.yml` (4 jobs delegan a esos stages; job database con servicio postgres:17). Verificado local: `scripts/ci.sh` verde, `actionlint`+`shellcheck` limpios. Pendiente: corrida real en Actions tras 1er push (D4). **Fase 0 casi cerrada** — sólo falta F0-02 (necesita Docker: pedir a Noel `supabase start`). Siguiente fase desbloqueada sin Docker: F1-01 (schema `brain_events`+RLS) puede AUTORARSE, pero su verificación (pgTAP, `db reset`) necesita Docker/DB → depende de F0-02.
- 2026-07-12 · F0-02 ✅ + F1-01 ✅ (Docker 29.6.1 resuelto; Supabase local vivo, `.env` seteado). **Fase 0 COMPLETA.** brain_events: migración + RLS (policies scoped a `auth.jwt()->>'tenant'`, append-only por grants) verificada con `supabase db reset` (aplica limpio) + `supabase test db` (pgTAP 6/6 PASS, incl. lectura/insert cross-tenant RECHAZADOS → L-ARCH-1). `scripts/ci.sh` endurecido: leak stage salta si falta `pg_prove` (corre vía container en `supabase test db`; su cableado en CI = F1-05). Nota: migración NO idempotente (create table) — CI usa DB fresca; dev usa `db reset`.
- 2026-07-12 · **D2 ✅** (Claude Agent SDK, confirmado). **F1-02 ✅** tool MCP `brain-write` construido (cero deps, Go stdlib): `control/cmd/brain-mcp` + `internal/mcp` (JSON-RPC stdio) + `internal/brain` (mintea JWT tenant HS256 → POST Supabase REST; **RLS real, sin service_role** — elección de auth del fundador). tenant/project inyectados por env; el agente sólo pasa kind/payload/actor. Verificado: unit tests + integración vs Supabase local (cross-tenant read/insert RECHAZADOS vía REST) + e2e stdio (fila con provenance confirmada en DB, id 8). Próximo desbloqueado en Fase 1: **F1-04** (brain explorer UI — requiere bootstrap console Next.js) o **F1-05** (leak test bloqueante en CI — su corrida real necesita push/D4). F1-03 (provenance req→issue→PR) depende de Fase 5 (handoff a Issues). Sin push (D4). `.env.example` documenta `SUPABASE_JWT_SECRET` + `FLUXO_TENANT_ID/PROJECT_ID`.
- 2026-07-12 · **Autonomía endurecida** (Noel actualizó CLAUDE.md: defaults firmes, dev-shim de auth OK, no re-preguntar ordenamiento/checkpoints/decisiones). **F1-04 ✅** console Next.js 15 + brain explorer `/brain/[projectId]` (RLS+Realtime directo a Supabase, sin backend; dev-shim del claim tenant). Agregué `brain_events` a la publicación `supabase_realtime` + `realtime.setAuth`. Verificado: build/tsc/lint + `verify-brain.mjs` 4/4 (RLS read, cross-tenant oculto, realtime entrega INSERT) + dev server 200. closes L-ARCH-4. **F1-05 ✅** leak test bloqueante en CI vía Supabase CLI (arregla bug latente postgres:17); mecánica probada local (verde → inject leak → exit 1/rojo → fix → verde); actionlint limpio. `scripts/ci.sh`: stages ahora `registry·control·db·console` (consolidé migrations+leak en `db`=`supabase test db`). **Fase 1: F1-01/02/04/05 ✅; F1-03 [~] bloqueada por Fase 5.** Siguiente: **Fase 2** (F2-01 schema stories/runs/events/sprints + RLS). El flip a required-check y la 1ª corrida real de Actions esperan D4 (push).
- 2026-07-12 · **FASE 2 COMPLETA** (F2-01/02/03/04 ✅). F2-01 schema sprints→stories→runs+events con RLS por tenant + FKs compuestas `(project_id,id)` (anti cross-project) + índices; pgTAP 13/13. F2-02 máquina de estados de story: transiciones legales **en datos** (`story_status_transitions`) + trigger = único punto de mutación (mata L-CQ-2); pgTAP 8/8, probado que dropear el trigger rompe 3 asserts. F2-03 endpoints `/stories`·`/runs` en `control` sobre RLS (store con token del tenant, sin WHERE tenant en Go) + status como constantes tipadas + **lint arquitectónico como go test** (status-literal + acceso REST directo); e2e con binario real: A ve su story, B ve `[]`, sin token 401. F2-04 leak stories/runs en el gate `db` bloqueante (probado que muerde). Siguiente: **Fase 3** (Maestro reconciliador: F3-01 Edge Function receptora de webhooks firmados). Recordatorio: sin push (D4); required-check flip espera remoto.
- 2026-07-12 · **FASE 3: F3-01/02/04 ✅; F3-03 [~]** (guard hecho, applier con F4/F5). F3-01 Edge Function `github-webhook` (HMAC firma + idempotente por `delivery_id` UNIQUE; tabla service-only; e2e con `functions serve`: 200/duplicate/401). F3-02 **kernel del Maestro en Go** `control/internal/maestro` (pure/total, no-LLM): `Decide` (única democión = evento terminal explícito → histéresis), `ShouldDispatch` (read-lag guard: live run → NO 2º dispatch + cooldown), `Normalize` (GitHub→Event, nunca inventa failure); 20+ tests. Placement Go por golden rule 6 (sin deno para testear); doc 01 actualizado. F3-03 guard (`ShouldDispatch`) ✅+testeado; el dispatcher real espera F4-01 (Runtime) + F5-03 (mapping repo→story). F3-04 proyección Realtime: `stories`+`runs` a la publicación + `replica identity full`; verificado que una transición llega a un subscriber por tenant sin polling (L-ARCH-4). Siguiente: **Fase 4** (Capa de Runtime: F4-01 interfaz `Runtime` + `Policy`). Sin push (D4).
- 2026-07-12 · **Fase 4 (selección) ✅: F4-01/02/06.** F4-01 interfaz `Runtime` + `Registry` (ids abiertos) + `Policy.Select` (fallback ordenado por lane). F4-02 `registry/providers/{claude,copilot}.yaml` + `claude_ephemeral.md`, loader `LoadProviders` genérico (yaml.v3, sin switch por id). F4-06 `Dispatcher` (fallback por lista + probe fail-open + trail de intentos para UI; threadea invoke data). Todo verificado con runtimes fake. **Boundary de infra externa:** F4-03 (github_actions) necesita **GitHub App + installation token (Vault/L-SEC-3) + repo cliente** — subsistema de credenciales BYO no construido, requiere crear la App (acción del fundador; liga a D4/F9). F4-04 (local_daemon) necesita el CLI local; F4-05 (docker_isolated) es local (Docker disponible). Localmente-hacibles sin infra: F4-05, F5-02 (backlog schema a data), F6 (UI console). Necesitan infra/decisión externa: F4-03, F5-01 (instalar Agent SDK), F5-03 (repo GitHub cliente), F7 (verify real), F8 (deploy/mobile/tiendas), F9 (onboarding/App), F10 (Rosa e2e).
- 2026-07-12 · **F4-05/04 ✅ + F5-02 ✅ + F6-01 ✅ + F3-03 ✅** (autonomía plena; D4 ✅ = aiudalabs/fluxo privado, pusheando). F4-05 docker_isolated (Docker real, egress-deny `--network none` verificado). F4-04 local_daemon + Daemon (reclama unidades) — corre CLI local, liveness de proceso. F5-02 backlog method a data (`registry/methods/{scrum,kanban}.yaml` + `control/internal/method`; kanban sin sprints = cero Go). F6-01 board Next.js (`/board/[projectId]`, realtime + deps + click-despachar) vía **función DB `dispatch_story`** (atómica: live-run guard + máquina de estados, RLS) → cierra F3-03. Lint status-literal ahora excluye `internal/runtime` (dominio distinto). CI: stages `registry·control·db·console`; jobs con setup-cli. Required-check flip espera branch protection (plan pago).
- 2026-07-12 · **F5-01 ✅ (Agent SDK instalado + corrida real).** Runtime de diseño `design/` (Node/TS, `@anthropic-ai/claude-agent-sdk`): agentes de fase corren por SDK con rol `.md` como system prompt; resolver `$step.output.text` (fix L-D2, 7 tests). Auth por `CLAUDE_CODE_OAUTH_TOKEN` (SDK spawnea CLI; nunca hardcodeado; en prod → Vault). Verificado REAL: analyst → brief 4809 chars (caso Rosa). CI: stage `design` (tests puros + tsc, sin API). Directiva de output redirige el doc al texto de respuesta. **Desbloquea F6-02 (Studio), F6-03 (brain trail), F5-04 (gates).** Próximo desbloqueado sin más infra: **F6-04** (i18n/rebrand), F5-04/F6-02 (Studio+gates sobre el runtime de diseño). Siguen esperando infra: F4-03/F5-03 (GitHub App+repo cliente), F7/F8/F9/F10.
- 2026-07-12 · **Refactor IA console → project-first (D6).** Rutas ahora `app/projects/[projectId]/{studio,board,brain}` + `app/projects` (lista); **layout de proyecto** (`lib/project.tsx · ProjectShell`) arma cliente supabase + tenant token en realtime **una sola vez** y las features lo leen de `useProject()` (dejan de re-armarlo). Retiré las rutas viejas feature-first. Actualizé home/links, i18n (nav.*/projects.*), README (convención), 01-arquitectura L3, 06-decisiones (D6) y los verify scripts (route-independent, nota de ruta nueva). Verificado: tsc+lint+build (5 rutas) + smoke dev (todas 200: `/projects/<uuid>/{studio,board,brain}`; la vieja `/brain/<id>` → 404) + `verify-brain.mjs` (data-path RLS+Realtime intacto, solo cambió la ruta). La URL pasa a `http://localhost:3000/projects/<uuid>/brain`.
- 2026-07-12 · **F6-03 ✅ + F6-04 ✅ (cerré lo local de Fase 6).** F6-03 brain explorer completo: rendering legible por kind + filtros + panel Trazabilidad requisito→issue→PR (la data llega con F5-03; presentación lista + empty-state honesto); verificado con `verify-brain-trail.mjs`. F6-04 i18n es/en dependency-free (`console/lib/i18n.ts` + `locale.tsx` con `LangToggle` runtime, español-first) + rebrand (UX/docs ya Fluxo); `.vibeforge-gate` del registry queda como deferral de F7 (contrato de método, no UX). **Fase 6 COMPLETA (F6-01/02/03/04 ✅).** Local-doable agotado en el spine de diseño; el próximo trabajo real (F5-03/F4-03, F7/F8/F9/F10) está en el **boundary de infra** (GitHub App + repo cliente / CI-deploy real) que setea Noel.
- 2026-07-12 · **F5-04 ✅ + F6-02 ✅ (el spine de diseño gateado).** F5-04 engine de orquestación puro `design/src/engine.ts` sobre ports inyectados: gate en **3 vías** (approve / revise-feedback / **answer preguntas abiertas**), loop acotado por `on_fail.max`, `$<gate>.detail` resuelto del data; `workflow.ts`/`openquestions.ts`. F6-02 **workdir-harvest (D5)**: `agent.ts` escribe a workdir (cwd + tools) — se retiró `OUTPUT_DIRECTIVE`; `harvest.ts` cosecha por diff (multi-archivo/multi-artefacto gratis); estado en Postgres (`design_runs/phases/gates` + RLS + Realtime, pgTAP 13/13); ports Supabase (`supabase.ts`: gate freeze poll-hasta-resolver + `gate_answer` al brain); **Studio UI** `console/app/studio/[projectId]` (timeline por Realtime, viewer doc/mockup, GatePanel conversacional, link al board al publicar backlog → cierra L-UX-1). Verificado de verdad: 31 tests design + tsc + build/lint console + **corrida SDK REAL** (analyst → `docs/BRIEF.md` cosechado) + `verify-gate-loop.ts` e2e (gate loop + brain + RLS) + `verify-studio.mjs` (data-path Studio por Realtime). **Fase 5: F5-01/02/04 ✅; F5-03 [ ] infra-blocked** (GitHub App/repo cliente — lo setea Noel). **Fase 6: F6-01/02 ✅; F6-03/04 [ ] locales pendientes.** Próximo local-doable: **F6-03** (brain explorer completo) y **F6-04** (i18n/rebrand, bajo valor → al final del spine). Boundary de infra ya identificado: F4-03/F5-03, F7/F8/F9/F10.
