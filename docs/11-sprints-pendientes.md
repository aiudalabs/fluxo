# 11 · Sprints de pendientes (backlog groomed que maneja el build de Fluxo)

> El backlog de trabajo de Fluxo v2, agrupado en **sprints** por valor y dependencia. Sale de fundir:
> `03-roadmap.md` (fases sin cerrar) + `10-calidad-build-backlog.md` (hallazgos de Idearium) + la
> memoria de improvements + lo que salió operando en prod. Se marca a medida que se cierra.
>
> **Orden recomendado:** P1 → (P2 ∥ P3) → P4/P5 (UX/producto) → P6 (GTM) → P7 (finish line).
> **Regla:** verificá el estado REAL antes de tomar un ítem — el roadmap puede estar desactualizado.

## ✅ Cerrado reciente (no re-listar)
F5-03 handoff (repo+issues+deps) · F6P-03 mockups navegables (DocView) · despliegue prod con TLS ·
conductor idempotente + re-mint del JWT · incongruencia de Settings (`execution_unit` + surface de
`dispatch_mode`) · workflow_approval `auto_if_safe` · vistas Agentes + Spend.

---

## 🟢 Sprint P1 — Confianza en el build (anti-stub + verify real) — **IMPLEMENTADO 2026-07-19**
**Objetivo:** que un backlog "verde" signifique *entregado*, no *cableado*. Es el moat y ataca de raíz
lo que encontró la validación de Idearium.

> **✅ Implementado y verificado en el repo real `nmlemus/misalon`** (commits df83c39 scaffold+verify,
> 3199dfb método anti-stub, + L-BUILD-1). El scaffold ahora emite **21 archivos** incl. el harness de
> verify completo (`.fluxo/verify/**` + e2e-verify/provisioning-lint/ui-verify), bloqueantes.
> **Aplicado a MiSalon** vía `rescaffold.ts` (idempotente, sin re-crear issues).

| # | Ítem | Estado |
|---|---|---|
| P1-1 | Verify determinista bloqueante | ✅ e2e-verify + provisioning-lint sin `continue-on-error` (guard interno → skip-limpio en repos sin `.fluxo/verify`). **Nota:** no se fuerzan required-checks en branch-protection (path-filters → deadlock); el auto-merge exige CLEAN. Bajo **merge_mode:manual** (MiSalon) el rojo es advisory → confirmar que el harness skipea-limpio en repos inmaduros ANTES de activar auto-merge. |
| P1-2 | CI ejecuta lo que protege | ✅ **CERRADO.** e2e-verify corre el sistema integrado + nuevo **`test-verify.yml`** (per-stack) **ejecuta la unit-suite del repo** bloqueante. El audit por ejecución de MiSalon lo probó necesario (37 tests que el CI nunca corría) y encontró **L-BUILD-2** (tests no herméticos: `ECONNREFUSED 127.0.0.1:5432` — unit tests que abren Postgres real; verde solo con infra levantada). Ver `docs/10`. |
| P1-3 | Método anti-stub | ✅ Reviewer (`claude-review.yml`): 4ª categoría de BLOCKER. Skill `acceptance-self-audit` item 5. Las **6 personas** de build (3 stacks ×2) con el mismo item 5. |
| P1-4 | Métricas ≠ retorno de stub | ✅ Incluido en el gate del reviewer + self-audit ("nunca leer una métrica de éxito del retorno de un stub"). |
| P1-5 | `ui-verify` deja de skipear | ✅ `ui-verify.yml` emitido y corre (react-supabase self-guard en `package.json`). *Diferido:* `app_path` (stack Flutter) + pre-render de `design_tokens` (un-skipea `frontend.instructions`). |
| P1-6 | Secretos fail-closed | ✅ En el gate del reviewer + self-audit ("nunca bootear prod con un secreto de dev; fail closed"). |
| P1-7 | **L-BUILD-1** en `04-lecciones` | ✅ Agregada (liga L-AUTO-3). |

> **Follow-up de P1 (deferido, NO bloquea):** el **generador de pre-render** de `design_tokens`/`path_map_*`/
> `validation_commands` — mientras no exista, `AGENTS.md`/`CLAUDE.md`/`frontend.instructions.md` se **saltan**
> (el scaffold los reporta con las vars que faltan). El reviewer + self-audit + `.agent.md` ya cubren el
> camino de build real. Va a **P4** (junto al ArtifactView).

---

## 🟢 Sprint P2 — Verificación de juicio (lo que el lint no ve) — **~IMPLEMENTADO 2026-07-19**
**Objetivo:** verificar que lo construido *funciona de verdad y se ve bien*, no solo que compila.

> **Verificado 2026-07-19:** P2 estaba **~80% construido por P1** (el harness ya traía el art-director y
> el e2e-verify). El trabajo real fue reconectar el juez-visión (P2-1).

| # | Ítem | Estado |
|---|---|---|
| P2-1 | Juez-visión: screenshot vs mockup aprobado; visual roto bloquea | ✅ (commit d0e7ced) El art-director de `ui-verify` (render screenshot+mockup → opus-4-8 → PASS/FAIL → rojo, con guard anti prompt-injection) ya estaba shippeado en P1 pero **skipeaba siempre** (buscaba `docs/mockups/<screen_key>.html`, el designer emitía un `index.html`). Fix del **contrato**: `ux` da la key `role.screen` por pantalla, `designer`/`mockup-html` emiten `docs/mockups/<screen_key>.html` per-pantalla, el `scrum-master` ya asigna `screen_key`. Ahora el juez dispara. **Aplica a runs futuros** (MiSalon ya tenía index.html → re-correr mockups para activar, opcional). |
| P2-2 | Verify cross-lane e2e | ✅ **Built + bloqueante en P1.** `e2e-verify` bootea el backend real, siembra, corre el flow en browser real + invariantes, exit 1 en fallo. Eso **es** L-LM-5 (integración = gate, no lint estático). El "desplegado↔desplegado" literal es **F8/P3**. |
| P2-3 | Verify multi-superficie (per-app) | 🟡 **PARCIAL.** Para **react-supabase** (web única, ej. MiSalon): las superficies son pantallas → cubiertas por el art-director per-`screen_key` (P2-1) + rutas por los e2e flows (el harness soporta N flows en `e2e.flows/`). **Residual:** el `ui-verify` de **Flutter** usa un solo `{{app_path}}` (primario) → un producto multi-app (customer+provider+admin) solo verifica el primario (L-LM-4). Fix diseñado: matrix sobre app_paths + flows por superficie. **Diferido** hasta que corra un proyecto multi-app real (no shippear una matrix untested a un stack sin proyecto vivo — disciplina anti-stub sobre lo nuestro). |

---

## 🟢 Sprint P3 — Última milla (que el app entregado corra) — **~IMPLEMENTADO 2026-07-19 (D3=cut)**
**Objetivo:** cerrar idea→**app viva**, no solo idea→PR. **D3 decidida: CUT** (web + link de prueba;
tiendas + provisioning-auto → v1.1). Ver `06-decisiones`. Deploy REAL = **BYO-credencial** (el humano
pone sus tokens Vercel/Railway).

| # | Ítem | Estado |
|---|---|---|
| P3-1 | Provisioning de infra del cliente | 🟡 **Cut minimal:** el cliente trae sus cuentas (Supabase/Vercel/Railway); el `deploy.yml` cablea env/secrets y publica contra ellas. **Provisioning AUTO (crear su Supabase vía API) → v1.1** (parte "full" de D3). |
| P3-2 | Deploy web preview + prod, "publicar con un click" | ✅ **`deploy.yml`** (react-supabase + python-fastapi-react): `workflow_dispatch` preview/prod, frontend→Vercel, backend/worker→Railway. Cada tramo skipea-limpio sin su secret (aditivo). Cierra F8-02/L-LM-2 (web). |
| P3-3 | Build móvil firmado + link de prueba | ✅ **`build-apk.yml`** (flutter): firma release real si hay keystore en secrets (cae a debug si no) + distribución a Firebase App Distribution si está configurado (cae al artifact si no). Cierra F8-03/L-LM-1 (cut). **Diferido:** iOS/TestFlight. *(flutter web deploy → hecho en Deploy A1, abajo)*. |
| P3-4 | Data migrations en el deploy | ✅ El `deploy.yml` corre migraciones ANTES del código (drizzle-kit / alembic), guardado por el secret de DB. Cierra F8-05. |
| P3-5 | Submission a tiendas | ⛔ **Diferido a v1.1** por D3 (cut). App Store Connect / Play Console, signing Apple, macOS runner. |
> **📌 Serie «Deploy · A» (última milla mobile, D7 BYO — ancla durable, recuperada del transcript de planificación 2026-07-20):**
> el stack Flutter aterriza el modelo D7 (deploy·preview·**verify**) contra la infra del cliente. El secret
> `FIREBASE_SERVICE_ACCOUNT` es **umbrella** (Hosting y Test Lab usan el mismo SA, distintos roles IAM). Orden fijado:
> **A1 primero** (más valioso + auto-contenido) → **A3 último** (el más pesado). Los tres:
> **· A1 — Flutter-web preview → Firebase Hosting** (el cliente ve/toca la app en el browser). ✅ hecho.
> **· A2 — Firebase Test Lab verify** (Google levanta el device, corre los integration tests de Flutter → screenshots/video/logs; BYO). ⚪ siguiente.
> **· A3 — N2 auto-provision** (OAuth + crear el proyecto Firebase en la cuenta del cliente vía API — el tramo «full» de D3/P3-1). ⚪ diferido, va último.

| **Deploy · A1** | **Flutter-web preview → Firebase Hosting (BYO)** | ✅ **IMPLEMENTADO** (2026-07-20 · `890a9e4`) — cierra el hueco que P3-3 dejó diferido y aterriza D7 para mobile. El stack `aiuda-flutter-firebase` gana **`deploy.yml`** (antes solo lo tenían react-supabase / python-fastapi-react), espejando ese patrón: `workflow_dispatch` con `target: preview\|prod`, tramo BYO que **skipea limpio** sin su credencial (aditivo). **preview** = `firebase hosting:channel:deploy` → URL temporal al `STEP_SUMMARY` (*la preview que el cliente toca en el browser*, D7); **prod** = `firebase deploy --only hosting`. **Cero secrets nuevos:** autentica con el **mismo** `FIREBASE_SERVICE_ACCOUNT` que ya declara la capability (P6-2b) vía `GOOGLE_APPLICATION_CREDENTIALS`, y el `project_id` sale del propio JSON del SA. Guard skip-clean = secret + `{{app_path}}/pubspec.yaml` + `firebase.json`. **Declarativo:** *qué* publicar vive en `firebase.json` del repo (`docs/provisioning.yaml` declara Firebase Hosting como target del frontend), igual que react-supabase mapea Vercel — cero metodología en código. `registry/capabilities/firebase.yaml` ahora **nombra el rol `roles/firebasehosting.admin`** en guía+descripción del secret (data), para que el self-serve no siembre un SA solo-Firestore y choque con un permiso indiagnosticable. **Multi-app:** deploya SOLO el `{{app_path}}` primario; la matrix customer+provider queda **diferida y nombrada** (L-LM-4 / P2-3) — no shippeamos una matrix untested. Suite: design **170** (piso 169 + 1), console **20**, tsc limpio (design+console), `validate.py` OK, next build OK. El test nuevo scaffoldea el stack Flutter **contra el registry real** y asierta `deploy.yml` con preview+prod, el skip-clean por `FIREBASE_SERVICE_ACCOUNT`, YAML válido con `jobs` y cero vars sin resolver. **Pendiente (no bloquea):** verify server = Firebase Test Lab (D7) → **es A2, abajo**; iOS/TestFlight. |
| **Deploy · A2** | **Firebase Test Lab verify (BYO)** | ✅ **IMPLEMENTADO** (2026-07-21 · `8550234`) — el tramo *verify* que D7 define para mobile (docs/06 §mobile) y A1 no cubrió. El stack Flutter gana **`device-verify.yml`**: compila los **integration tests** de la app primaria (`{{app_path}}/integration_test`, app + androidTest APK vía `gradlew assembleAndroidTest`/`assembleDebug -Ptarget`) y los corre en **Firebase Test Lab** (device-farm gestionado de Google) sobre el proyecto Firebase **del cliente** (BYO, cero COGS) con `gcloud firebase test android run --type instrumentation` → video/screenshots/logs. Es el equivalente mobile del `e2e-verify`/`ui-verify` (Playwright) que ya cubre web. **Mismo secret umbrella** `FIREBASE_SERVICE_ACCOUNT` (P6-2b) vía ADC; `project_id` del propio JSON del SA (cero secrets extra), igual que A1. **Disparo `workflow_dispatch` (manual):** a diferencia de `ui-verify`/`test-verify` (on-PR, gratis en runners de GitHub), Test Lab corre en el Firebase del cliente y **cuesta por ejecución** → on-PR sería sorpresa de costo (misma disciplina que `build-apk` / `dispatch_mode:manual`). **Skip-clean BYO:** sin el secret, sin `{{app_path}}/pubspec.yaml` o sin `integration_test/*_test.dart` → skip limpio (aditivo). **Device por default de Test Lab** (sin `--device`: un `model/version` se pone stale con el catálogo de Google); **matrix de dispositivos configurable diferida** (v1.1). **Multi-app:** solo el `{{app_path}}` primario + el primer test como driver; matrix customer+provider **diferida y nombrada** (L-LM-4 / P2-3). `registry/capabilities/firebase.yaml` ahora nombra los roles del verify — **Firebase Test Lab Admin** (`roles/cloudtestservice.testAdmin`) + **Firebase Analytics Viewer** (`roles/firebase.analyticsViewer`) — en guía+descripción del secret (data), como A1 con Hosting Admin. Alternativa rechazada: `android-emulator-runner` en Actions (pesado/flaky). Suite: design **171** (piso 170 + 1), tsc limpio, `validate.py` OK (templates 56→57). El test nuevo scaffoldea el stack Flutter **contra el registry real** y asierta `device-verify.yml` con `workflow_dispatch`, `integration_test`, `assembleAndroidTest`, `gcloud firebase test android run --type instrumentation`, el skip-clean por `FIREBASE_SERVICE_ACCOUNT`, YAML válido con `jobs` y cero vars sin resolver. **Pendiente (no bloquea):** correr contra un Firebase real (requiere el SA con roles de Test Lab + integration tests en el repo); matrix de dispositivos/superficies. |
| **Deploy · A3** | **N2 auto-provision (OAuth + crear proyecto Firebase)** | ⚪ **DIFERIDO v1.1 · DISEÑO HECHO** (2026-07-21) — el tramo más pesado (va último). OAuth con Google + crear el proyecto Firebase/GCP en la cuenta del cliente vía API (Resource Manager `projects.create` + Cloud Billing + Firebase Management `addFirebase` + IAM key-mint → produce el **mismo** secret `FIREBASE_SERVICE_ACCOUNT` umbrella que A1/A2 ya consumen). Es el «full» de D3 que **D3+D7 difieren a v1.1** (docs/06 D7 L79). Rompe el patrón puramente-BYO-secret (Fluxo pasa a tener un OAuth app propio). **Diseño + 5 decisiones abiertas ⚠️ en `docs/13-A3-auto-provision-diseno.md`** — bloquean el build (sobre todo OAuth app + verification de Google, y storage del token/Vault). NO se construyó nada: cero OAuth registrado, cero secrets, cero código. |

> **Estado de aterrizaje en MiSalon:** el harness de verify (incl. `test-verify`) ya está en `nmlemus/misalon`
> (rescaffolds previos). `deploy.yml` quedó pendiente de subir por un **HTTP 503 de GitHub** (blip externo,
> no del código) — reintentar el `rescaffold.ts` cuando GitHub se recupere. El deploy real necesita los
> tokens del usuario (Vercel/Railway) de todos modos.

---

## 🟢 Sprint P4 — ArtifactView + Observabilidad
**Objetivo:** que todo lo que Fluxo produce sea legible y trazable en la UI.

| # | Ítem | Estado |
|---|---|---|
| P4-1 | **ArtifactView** completo | ✅ (2026-07-19, deployado) `DocView` resaltea yaml/json/shell (tokenizer sin dep, spans seguros, tokens theme-aware) y lo adoptan **Studio + Registry + Brain**. Un solo renderer por tipo. Resuelve "yaml/.vibeforge-gate como texto crudo". |
| P4-2 | **Observabilidad** estilo Langfuse/Arize | ✅ (2026-07-19, deployado + migración aplicada a fluxo-prod). **A)** `agent.ts` captura costo/tokens/latencia del SDK por fase → `design_phases` (migración `20260719120000`); worker instrumentado (verificado sin crash). **B)** vista **Observabilidad** (rename de Brain): dashboard (costo total/diseño/build + tokens + fases·runs) + tab **Trazas** (fases con barra de duración+costo + gates como anotación + builds de `run_costs`) + tab **Eventos** (log preservado). El costo de DISEÑO aparece "—" en MiSalon (fases pre-instrumentación) → se llena en el próximo design run. |
| P4-3 | Drawers de nodo en `/flow` | ✅ (2026-07-19, deployado) Click en nodo de story (`story:<id>`) → abre el `TicketDetail` del board (reusado; deps clickeables, PR/sesión/run). Fases/sprints no abren drawer (sprint ya linkea al board). F6P-04. |
| P4-4 | Decisión Sidebar 11 secciones | ✅ **Decidido (2026-07-19): mantener el top-nav project-first de v2** (ratifica D6). NO se replica el sidebar global de v1 — el IA project-first es el correcto para una herramienta project-scoped y ya estaba validado. Cero código. Cierra F6P-05. |

---

## 🟢 Sprint P5 — AI Assistant + change-requests — **COMPLETO 2026-07-20**
**Objetivo:** el bot agéntico que v1 tenía y v2 perdió, + el disparador de incrementos.
**Los 3 ítems hechos + deployados.**

| # | Ítem | Fuente |
|---|---|---|
| P5-1 | **AI Assistant** agéntico | ✅ **COMPLETO (2026-07-20, deployado).** Chat con las 3 acciones confirmables (increment/dispatch/gate), UI de v1 + markdown, sección + panel flotante. Streaming SSE = polish diferido. *(detalle abajo)* Fundación VIVA y de-riesgada. Diseño cerrado: LLM = token de suscripción vía claude-agent-sdk; acciones (incremento/dispatch/gate) con confirmación; UI sección+flotante. **Infra de-riesgada:** el agent-loop corre en el **worker** (ambiente probado), el console **proxea** (`WORKER_ASSISTANT_URL`) — no en el alpine/root del console. v1 DEPLOYADO + probado (respondió con datos reales de MiSalon). Sección "AI Assistant" en la nav, **UI portada de v1** (`.brain*`) con respuestas en **markdown**. **Acción "pedir incremento" con confirmación LISTA:** el bot emite un bloque ```fluxo-action``` (JSON validado) → la UI lo renderiza como tarjeta `.brain-action` → "Confirmar y pedir" → encola en `increment_requests` (patrón propose→confirm→execute, el bot nunca dispara solo). Probado E2E (propuso "Mis Citas"/"depósitos" referenciando las stories reales). **Próximos incrementos:** acciones dispatch/aprobar-gate (hoy sugeridas en prosa) + panel flotante + streaming SSE. |
| P5-2 | Botón **"pedir incremento / change-request"** | ✅ (2026-07-19, deployado) Vertical slice: tabla `increment_requests` (cola, RLS) + `worker.reconcileIncrements` → `spawnIterate` (main.ts --workflow=iterate) + `main.ts` siembra el workdir con los docs existentes (`loadProjectDocs`) → el `iteration-planner` emite un DELTA → handoff APPENDea. UI: componente `IncrementRequest` (textarea + lista Realtime) en el Overview cuando el producto existe. El motor `iterate.yaml` ya existía; esto es el disparador. **Primer uso real dispara un planner pago** (aún no corrido). |
| P5-3 | Selección de **workflow por proyecto** en Settings | ✅ (2026-07-20, deployado) El worker usaba un flag global `--workflow=design`; ahora cada proyecto elige en Settings **Completo (`design`, 8 fases)** vs **Lean (`demo-design`, 3 fases)**. `reconcileDesign` lee `settings.workflow` → `spawnDesign` (solo fresh; resume usa el del run). |
| P5-4 | **Memoria del AI Assistant** (persistencia de conversación) | ⚪ **PENDIENTE.** Hoy el chat es *stateless entre sesiones*: `AssistantChat` guarda los mensajes en `useState([])` (se pierden al refrescar/navegar), no hay tabla de historial, y `runAssistant` (worker) recibe la historia por request y no persiste nada. Dentro de un hilo el bot tiene contexto; al volver arranca de cero — no recuerda charlas ni decisiones. **v1:** tabla `assistant_threads`/`assistant_messages` (RLS tenant+project), load-on-mount, el worker appendea la respuesta. **v2 (opcional):** memoria semántica del proyecto que el bot pueda leer/escribir (estilo OMEGA). Chico; candidato a colgar de P8 o hacerlo como P5-follow-up suelto. |

---

## ⚪ Sprint P6 — Onboarding + GTM (para vender)
*(Del roadmap Fase 9.)*

| # | Ítem | Fuente |
|---|---|---|
| P6-1 | Wizard de onboarding con **semáforos de capacidad reales** | F9-01 · L-UX-3/4 |
| P6-2 | Capabilities project-scoped + **canal Copilot real** | F9-02 · L-UX-2 |
| P6-2b | **Diseño capability-aware + Capabilities BYO** — 🔴 **CORRECTITUD, no GTM** (el E2E lo probó el 2026-07-20). S-fbmig-1 nació con un AC **no-despachable**: *"Existe el proyecto Firebase Blaze + billing"* — ningún agente crea un proyecto GCP+billing. **Una story de build con provisioning humano adentro es un BUG de diseño** que rompe el self-serve para TODO stack con provisioning (Firebase/Supabase/Stripe/Vercel…). ⚠️ **NO parchear a mano** (editar el AC / sembrar la key por sesión) — eso me necesita a MÍ y el producto es self-serve ([[fluxo-no-manual-patches-self-serve]]). Solución sistémica, 3 piezas: **(1) diseño capability-aware** — el stack declara en `registry/` qué es provisioning-humano-one-time vs build; el architect lo vuelca a `docs/provisioning.yaml` (seam ya existe: `design.yaml` lo pasa a la fase backlog); el scrum-master emite stories con SOLO ACs que el agente cumple (build+test contra emulador, deploy contra el proyecto concedido). **(2) onboarding/capabilities self-serve** (P6-1/P6-2) — checklist con semáforos reales 🟢: el usuario crea su proyecto (link guiado) + pega la service-account key → Actions secret (canal BYO como el token de Claude). **(3)** el agente deploya contra el proyecto del usuario. Data-driven (golden rule #1/#5). | 2026-07-20 · el E2E lo probó · extiende P3 BYO-deploy |
| P6-2b · Paso 1 | ✅ **IMPLEMENTADO** (2026-07-20) — **Backlog capability-aware (método/registry + gate determinista).** Pieza (1) de las 3. **(a) Capabilities como data:** `registry/capabilities/firebase.yaml` (`provisioning`{summary,guide,markers}, `secret` FIREBASE_SERVICE_ACCOUNT, `probe`, `emulator:true`) + `registry/capabilities/README.md` (schema) + `registry/stacks/aiuda-flutter-firebase.yaml` (el stack declara `capabilities:[firebase]`, fuera del árbol que scaffold shippea). **(b) Contrato de frontera:** `architect.md §8` + schema `provisioning.yaml` ganan la grada **`accounts:`** (proyecto/cuenta+billing que crea el HUMANO one-time, nunca un AC de build). **(c) Regla del scrum-master** (§3c-bis + PO-checklist): un item de `accounts:` NUNCA se re-enuncia como AC; las stories referencian el secret (`deploy usando $FIREBASE_SERVICE_ACCOUNT`) + testean contra el emulador. **(d) Gate determinista** (design-time, `capabilities.ts::resolveFrontierMarkers` + `repodocs.ts::findProvisioningLeaks` → `planRepoDocs.provisioningLeaks` → `handoff.ts` reporta `handoff_backlog_provisioning_leak` al brain; **reporta-no-traba** como `uncoveredScreens`/`missingMockups`). **Test PRIMERO** con el caso real S-fbmig-1 (AC "proyecto Firebase Blaze + billing" cazado; el AC referenciado NO). Suite: design **146** (piso 132 + 14) verde, console 8, tsc limpio, `validate.py` OK. Verificado E2E contra el registry real. **Pendiente P6-2b:** Paso 2 (onboarding/capabilities self-serve 🟢) + Paso 3 (readiness gate en el dispatch). | 2026-07-20 · Paso 1 de P6-2b |
| P6-2b · Paso 2 | ✅ **IMPLEMENTADO** (2026-07-20) — **Capabilities self-serve (checklist + probe 🟢 + siembra).** Pieza (2) de las 3. Decisión de UX: **extender Settings → «Canal de build»** (un solo lugar Integraciones), NO una sección nueva. **(1) Resolver data-driven** (`design/src/capabilities.ts::resolveProjectCapabilities` + `parseStack`): del `stack` de `docs/provisioning.yaml` (venido del brain, `brain_events` kind=artifact, última versión del path — la misma fuente del Studio) → `registry/stacks/<stack>.yaml` `capabilities:` → `registry/capabilities/<cap>.yaml`; cada una aporta `name`, `secret`, `provisioning.{summary,guide}`. **(2) GET generalizado** (`console/app/api/projects/[id]/channel/route.ts`): además del canal claude devuelve `capabilities:[{id,name,secret,guide,summary,status}]`; `status` 🟢=Actions secret presente / ⚪=falta (probe v1 = "secret presente", igual que el token de Claude; el probe pesado del YAML se difiere). Reusa el token refrescado. **(3) PUT generalizado**: acepta `{secret,value}` (legacy `{token}`→Claude intacto); ⚠️ **whitelist server-side** (`console/lib/server/capabilitiesData.ts::secretWhitelist` = `CLAUDE_CODE_OAUTH_TOKEN` ∪ secrets de las capabilities RESUELTAS del registro) — un secret arbitrario del cliente se **rechaza** (400). El valor NUNCA se persiste (BYO → `gh secret set`). **(4) UI** (`Settings.tsx`): checklist bajo el canal claude — guía + link + input + «Sembrar secret» + semáforo 🟢/⚪, reusando los patrones `.stg-*`. Suite: design **152** (piso 146 + 6) verde, console **13** (piso 8 + 5) verde, tsc limpio (design+console), next build OK, `validate.py` OK. E2E de data contra el registry real: stack `aiuda-flutter-firebase` → resuelve `firebase` (secret `FIREBASE_SERVICE_ACCOUNT`, guía, status ⚪); PUT acepta `FIREBASE_SERVICE_ACCOUNT`+Claude, rechaza `AWS_SECRET_ACCESS_KEY`. **Pendiente P6-2b:** Paso 3 (readiness gate en el dispatch: no despachar una story hasta que su capability esté 🟢). | 2026-07-20 · Paso 2 de P6-2b |
| P6-2b · Paso 3 | ✅ **IMPLEMENTADO** (2026-07-20) — **CIERRA P6-2b. Readiness gate por capability en el dispatch.** Pieza (3) de las 3. Una story que REFERENCIA el secret de una capability (ej. `$FIREBASE_SERVICE_ACCOUNT`) NO ofrece ▶ hasta que esa capability esté 🟢 (Actions secret presente); una story que solo construye/testea contra el **emulador** (no referencia el secret) NO se gatea. **(1) Kernel puro** (`dispatch.ts`): `candidates()` gana un 4º param `greenCapabilities: ReadonlySet<string>` (default vacío = backward-compatible) + `DStory.needsCapabilities` (por-story, poblado por el caller, precedente = `screenKey`). `storyReady` además exige `needs ⊆ green`; en sprint-mode un miembro backlog con need no-🟢 gatea TODO el sprint (unidad atómica goal-mode). Cero network en el kernel. **(2) Needs data-driven** (`capabilities.ts::storyNeedsCapabilities`): una story necesita X sii referencia el **nombre EXACTO del secret** de X (substring case-sensitive, del registry) en body/acceptance — match **conservador** (un falso positivo bloquea una story buildeable → preferimos NO gatear ante la duda). `computeCapabilityGate` arma `{needsByStoryId, green}` probeando SOLO las capabilities referenciadas; probe **tri-estado**: 404=ausente (gatea), presente=🟢, `null` (403/red/sin token)=**fail-open** 🟢 (como `docsGuardOk(null)`). **(3) Los 3 callers** resuelven green+needs y los pasan: `worker.ts::reconcileBuild` (app token, pre-check barato para saltear el I/O del gate en proyectos ociosos), `POST /dispatch` (money-safe, re-deriva), `GET /candidates` (board). Probe reusado (`GithubRepo.secretPresent` / `githubSecretProbe`). `provisioning.yaml` del brain (misma fuente que el channel route; `loadProvisioningYaml` centralizado). **(4) Board honesto**: `/candidates` devuelve `capWaiting` → la card pinta `⧗ esperando capacidad: Firebase` (reusa `kb-gate`) en vez de un ▶ a un stub; gate BLANDO (🟢 → ▶ reaparece). Suite: design **169** (piso 152 + 17), console **20** (piso 13 + 7), tsc limpio (design+console), next build OK, validate.py OK. **E2E de data contra el registry real** (capa GitHub mockeada): story que referencia `$FIREBASE_SERVICE_ACCOUNT` con firebase ⚪ → NO candidata (board «esperando capacidad»); 🟢 → candidata; story de emulador → candidata siempre. | 2026-07-20 · Paso 3 de P6-2b (cierra) |
| P6-3 | Billing de agencia + metering (plan Team, overage, BYO-key) | F9-03 |

---

## ⚫ Sprint P7 — Aceptación E2E (finish line)
| # | Ítem | Fuente |
|---|---|---|
| P7-1 | MiSalon/Rosa corre de **punta a punta** (idea→app viva→brain trazable→change-request re-entra) | F10-01 |
| P7-2 | Webhooks activos en prod (hoy el worker poll-ea; falta el receiver) | F10-02 |

---

## 🟢 Sprint P8 — Fidelidad diseño→build (que la UI entregada refleje el spec+mockups) — **IMPLEMENTADO 2026-07-20**
**Objetivo:** cerrar el hueco de método que dejó el panel del dueño de **MiSalon esquelético** (validación n=3).
**Diagnóstico raíz:** el `scrum-master` **comprimió 27 pantallas de `UI_SCREENS.md` en ~8 `screen_keys`** → pantallas
enteras nunca tuvieron story → nunca existieron para ningún agente. La falla es de la **cadena del método** (3 eslabones
rotos), no del implementador. Está **aguas arriba del harness de verify (P1/P2)**: el art-director solo juzga lo que tiene
story+mockup; si la story nunca existió, no hay nada que construir ni verificar. Prerequisito (**los mockups llegando al
repo**) **ya resuelto** por `repodocs.ts` (commit 95414c2). Complementa la línea de calidad de build (docs/10, L-BUILD).

| # | Ítem | Estado |
|---|---|---|
| P8-A | **Cobertura de UI en el backlog** | ✅ (2026-07-20, `b730465`) El `scrum-master` LEE `docs/UI_SCREENS.md` y emite una story por pantalla, con matriz `coverage:` (pantalla→story) + bloque `out_of_scope:` explícito en `backlog.yaml`. `design.yaml` pasa `screens_path` a la fase `backlog`. Degrada con gracia sin `UI_SCREENS.md` (igual que `provisioning.yaml`). **Llave de join = ID de pantalla verbatim del header** (`P.1`, `S.5`, `1.2`…), NO `screen_key`/`role.screen` — verificado n=6 que ningún `UI_SCREENS.md` real emite el dotted key; unir por él sería un no-op silencioso. |
| P8-B | **Chequeo determinista de cobertura** | ✅ (2026-07-20, `5e1c149` + `789c70e`) En el seam REAL (`repodocs.ts`/`handoff.ts`, NO el `validate` no-op del engine): `parseScreenIds` parsea `UI_SCREENS.md`, `parseCoverageClaim` lee `coverage`/`out_of_scope`, `planRepoDocs` reporta `uncoveredScreens`. El handoff lo emite como `handoff_screens_uncovered` (guard al brain, mismo estilo que `missingMockups` — **reporta fuerte, no falla** el handoff: parse heurístico no debe trabar el build). Tests primero (golden rule #6), **piso 123→132/132**. **Demostrado contra MiSalon real:** parsea las 27 pantallas y reporta el panel del dueño que faltó (S.6-9, S.11-14). |
| P8-C | **Puntero a spec+mockup en el prompt del dev** | ✅ (2026-07-20, `789c70e`) `storyPrompt`/`sprintPrompt` (`dispatch.ts`) e `issueBody` (`handoff.ts`) apuntan a la sección de la pantalla en `docs/UI_SCREENS.md` y a `docs/mockups/<screen_key>.html`, no solo "leé docs/". `dispatch.ts` sigue kernel-puro: `screenKey` llega por `DStory` (sin fs/imports); threaded en el select del worker y del console. |

> **Casa (constraint del pedido):** branch fresco, tests-first, **NO** incluir en los commits los cambios ajenos de otra sesión
> en `console/components/AssistantChat.tsx` ni `design/src/assistant.ts` (P5-1). Branch → ff-merge a main local, **sin push**
> hasta OK. Ver el spec completo en el mensaje de origen / `~/.devtrace/decisions/fluxo.md`.

---

## 🧹 Deuda chica (folder aparte, no bloquea sprints)
> **Barrida 2026-07-21:** 7 de 8 ítems resueltos con tests (commits abajo). El único que queda es **#1**
> (más grande que deuda-chica, toca la SM de proyección → tarea propia con atribución run→story).
- **Dispatch fuera del board no se trackea** ⏸ **NO resuelto — reencuadrado (más grande que deuda-chica).** Un re-run manual del Action en GitHub no promueve la story a `running`. El `Projector` ya deriva status de GitHub (PR/label/assignee) con histéresis, pero `liveRunCount` es **a nivel repo**, no por-story → promover `backlog/ready→running` por un run vivo movería TODAS las backlog con cualquier run. El fix correcto exige **atribución run→story** (el dispatch estampa el run con el issue/key; el projector lee estado de run por-story) diseñada con histéresis — tocar la SM de proyección sin eso reintroduce el "flap" de v1 (golden rule #2). **Tarea propia, no un parche.**
- **`publishBacklog` huérfanos al encoger** ✅ **RESUELTO** (2026-07-21 · `da4139b`). `publishBacklog(…, {full})` detecta huérfanas SOLO en re-handoff full (design): stories `backlog` que ya no están en el backlog aprobado → se **reportan** al brain (`backlog_orphans`, patrón P8-B), NO se borran (perdería trabajo despachado; DELETE revocado). `main.ts` pasa `full = workflowId !== "iterate"` (un iterate es delta aditivo → nunca huérfanos). Archivado duro (status `archived`) diferido a propósito: exige migración + filtro de board → reintroduciría drift. Tests: full reporta / iterate no.
- **Decisión de producto:** ✅ **RESUELTO** (2026-07-21 · `bb8b83f`) — el usuario decidió `manual`. El default global de `dispatch_mode` pasa de `auto` a `manual`: un proyecto sin setting explícito NO auto-despacha agentes pagos (el humano toca ▶). `worker.ts` gate `?? "manual"` + `Settings.tsx` DEFAULTS. Quien quiera `auto` lo activa en Settings.
- **Drift de migraciones local↔prod** ✅ **RESUELTO** (2026-07-21 · `373d5cf`). El worker corre `preflightSchema()` ANTES del primer tick: prueba (data-driven) que las tablas/columnas esperadas existen (`increment_requests.id`, `design_phases.cache_read_tokens`); si falta algo → mensaje accionable (qué + qué migración + "aplicá a ESTE entorno") y `exit(1)`. Falla rápido y claro al arrancar en vez de un 400 a mitad de un run pago. Verificado contra el PostgREST local real (presente→200, faltante→404/42P01). *(El "aplicar cada migración a local+prod en el mismo paso" es proceso, no código; el preflight lo hace seguro.)*
- **`iterate` pisa `docs/backlog.yaml` con el delta** ✅ **RESUELTO** (2026-07-21 · `26e90fd`). `store.loadBacklog()` reconstruye el backlog completo desde la DB (sprint uuid→key, deps blocked_by→keys); `serializeBacklog()` lo emite en el schema del scrum-master (round-trip con `parseBacklog`). En el handoff, si la DB quedó con MÁS stories que el workdir (era un delta) → regenera `docs/backlog.yaml` desde la DB antes de commitear. Un diseño fresco (mismos counts) NO se toca → preserva coverage/out_of_scope (P8-A). Tests: round-trip + loadBacklog.
- **`iterate` no corre la fase de mockups** ✅ **RESUELTO** (2026-07-21 · `de951f1`). `iterate.yaml` gana una fase `mockups` (designer) entre `plan_gate` y `handoff`, sin gate propio (el art-director de build-time es el gate real). `designer.md` gana un clause "modo incremento": con `backlog_path`, genera un `docs/mockups/<screen_key>.html` por pantalla NUEVA del delta (deriva del body/acceptance de la story — el planner no actualiza UI_SCREENS.md), lee los existentes y solo agrega lo que falta; backend-only → no-op. Backward-compatible (design.yaml no pasa `backlog_path`).
- **Studio en un incremental no mostraba los docs viejos ni las 2 versiones del backlog** ✅ *resuelto 2026-07-20.* Studio derivaba el panel de documentos de `phases` (SOLO el último run) → un `iterate` (que solo produce su fase delta) escondía PRD/BRIEF/backlog. Fix: `files` deriva del **brain versionado** (`brain_events` kind=artifact, project-wide) — la fuente de verdad (cada doc de diseño se sube a git + se registra como versión), con fallback a las fases del run actual. Ahora se ven todos los docs y sus chips vN.
- **Hardening del sink: `brainAppend` corre DESPUÉS del write de costos** ✅ **RESUELTO** (2026-07-21 · `d45d8ab`). `onPhaseDone` ahora: `brainAppend` de los artifacts PRIMERO (las versiones del Studio, lo durable), `patchPhase(status+artifacts)` durable, y `patchPhase(costos)` best-effort (try/catch, loguea y sigue). Un fallo del write de métricas (ej. drift de `cache_read_tokens`) ya NO pierde la versión del doc. Test: cost-write 400 → versión registrada, fase `done`, no lanza.
- **JWT expira → "Could not read the repo docs.: JWT expired"** ✅ **RESUELTO** (2026-07-21 · `c1e4b4a`). Refresh silencioso proactivo en el console: `POST /api/auth/refresh` verifica el JWT de sesión vigente → mintea uno fresco (no toca GitHub); `supabaseClient.freshToken()` lo cambia cuando quedan <60 min, debounced, cableado en `accessToken` → mientras el usuario esté activo el token nunca vence. Cubre el hueco del console (el re-mint del worker ya cubría el conductor). Tests: `decodeExp`/`needsRefresh`. `next build` OK.

---

*Estado: abierto. Fuente de verdad del build. Al cerrar un ítem, marcá `[x]` y dejá una línea de fecha·qué·commit.*
